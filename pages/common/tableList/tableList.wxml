<!--tableList.wxml-->
<import src="/pages/common/eachTableList/eachTableList" />

<template name="tableList">
  <block wx:for="{{tableData}}" wx:key="{{item}}">
    <view style="height: 300px">
      <template is="eachTableList" data="{{item: item, isRegister: isRegister}}"></template>
    </view>
  </block>
</template>